class HomeConstants {
  // Onboarding
  static const Duration onboardingDelay = Duration(milliseconds: 1200);
  static const String onboardingSeenKey = 'has_seen_reorder_onboarding';
  
  // Refresh
  static const Duration refreshDelay = Duration(milliseconds: 500);
  
  // Storage keys
  static const String selectedSourceKey = 'selected_patient_source_id';
  
  // Animation durations
  static const Duration pageTransitionDuration = Duration(milliseconds: 300);
  
  // Spacing
  static const double bottomPadding = 116.0;
}
